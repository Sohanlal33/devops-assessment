name: CI/CD Pipeline

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Docker login
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push backend image
        uses: docker/build-push-action@v5
        with:
          context: ./backend
          push: true
          tags: sohanlal33/devops-backend:v1.0.0

      - name: Build and push frontend image
        uses: docker/build-push-action@v5
        with:
          context: ./frontend
          push: true
          tags: sohanlal33/devops-frontend:v1.0.0

    deploy:
    runs-on: ubuntu-latest
    needs: build-and-push
    steps:
      - name: Configure AWS Credentials via OIDC
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::${{ secrets.AWS_ACCOUNT_ID }}:role/nexgen-github-oidc-role
          aws-region: ap-south-1

      - name: Run deployment commands on EC2 via AWS SSM
        run: |
          aws ssm send-command \
            --instance-ids "${{ secrets.INSTANCE_ID }}" \
            --document-name "AWS-RunShellScript" \
            --comment "Final stable deployment" \
            --region ap-south-1 \
            --parameters '{"commands":["cd /home/ubuntu/devops-assessment && sudo git fetch origin main && sudo git reset --hard origin/main","export DOCKERHUB_USERNAME=${{ secrets.DOCKERHUB_USERNAME }}","export DOCKERHUB_TOKEN=${{ secrets.DOCKERHUB_TOKEN }}","sudo -E bash deploy.sh"]}'



